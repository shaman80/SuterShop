<Window x:Class="SuterShop.Chat.View.ChatView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SuterShop.Chat.View" 
        xmlns:viewmodel="clr-namespace:SuterShop.Chat.ViewModel"
        mc:Ignorable="d"
        Title="ChatView" Height="450" Width="800">
    <Window.DataContext>
        <viewmodel:ChatViewModel/>
    </Window.DataContext>
    <Grid>
        <ListBox x:Name="ChatListBox" HorizontalAlignment="Left" Height="250" Margin="10,10,0,0" VerticalAlignment="Top" Width="560" ItemsSource="{Binding ChatMessages, UpdateSourceTrigger=PropertyChanged}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Margin="5" Width="100" Text="{Binding Sender}"/>
                        <TextBlock Margin="5" Text="{Binding Text}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBox x:Name="MessageTextBox" HorizontalAlignment="Left" Margin="10,270,0,0" VerticalAlignment="Top" Width="420"/>
        <Button Content="Отправить" HorizontalAlignment="Left" Margin="440,270,0,0" VerticalAlignment="Top" Width="120" Click="SendMessageButton_Click"/>
    </Grid>
</Window>
