<Window x:Class="SuterShop.Feedback.FeedbackView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:feedback="clr-namespace:SuterShop.Feedback"
        mc:Ignorable="d"
        Title="FeedbackView" Height="450" Width="800">
    <Window.DataContext>
        <feedback:FeedbackViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            
        </Grid.RowDefinitions>

        <TextBlock Text="Оставьте отзыв" FontWeight="Bold" FontSize="16" Margin="10"/>
        
        <TextBox x:Name="textBoxReview" Grid.Row="1" Margin="10,0,10,10" MinHeight="100" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

        <Button Content="Отправить" Grid.Row="4" Width="150" Height="30" HorizontalAlignment="Left" Margin="10,0,0,10" Click="SubmitReviewButton_Click"/>
    </Grid>
</Window>
